# DONE

Live: https://grok-bot-exploded.vercel.app/

Local preview:

```bash
npm install
npm run build
npm run preview
```

Open the URL `npm run preview` prints. The inline porcelain poster is the first paint. The stage replaces it when WebGL starts.

## Checked here

- `npm run build` exits 0 (tsc and vite).
- Content check: twelve poems, six FAQ answers in the page and in JSON-LD (FAQPage, TechArticle, HowTo, WebApplication), disclaimer, llms.txt, robots, sitemap, manifest, favicon, og.jpg.
- Cage geometry is a 60-vertex, 90-edge truncated icosahedron. The stadium eye mesh is wider than it is tall.
- Explode math: shell leads, cortical sphere finishes at 1, pipes stay coupled through 0.55.
- Production HTTP 200 at the alias above.

## Not watched in a browser from this terminal

A preview server was not left running. Click through explode, isolate, the keys, and the 390 px layout on the live URL or on `npm run preview`.

Deploy again with:

```bash
vercel --yes --prod
```
